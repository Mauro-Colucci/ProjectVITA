<div class="card">
           
    <h5 class="card-header"><%= singleProject.projectName %> </h5>
      <div class="card-body">
        <form>
            <div class="row">
                <div class="col-md-4 mb-3">
                    <a href="/profile/<%= singleProject.user._id %>" class="text-decoration-none text-reset">
                        <label  class="form-label">Username</label>
                        <input type="text" class="form-control" style="cursor: pointer;" name="Username" value="<%= singleProject.user.userName %> " disabled>
                    </a>
                </div>
                <div class="col-md-4 mb-3">
                    <label for="exampleFormControlInput1" class="form-label">Email address</label>
                    <input type="email" class="form-control" id="exampleFormControlInput1" value="<%= singleProject.user.email %>" disabled>
                </div>
                <div class="col-md-4 align-self-center text-center mb-3 mb-md-0">
                    <div class="btn-group">
                        <% if (singleProject.sourceCode != null) { %>
                            <a href="<%= singleProject.sourceCode %>" target="_blank" class="btn btn-outline-dark btn-lg"><i class='bx bxl-github'></i></a>
                        <% } %>
                        <% if (singleProject.liveDemo != null) { %>
                            <a href="<%= singleProject.liveDemo %>" target="_blank" class="btn btn-outline-dark btn-lg"><i class='bx bxs-invader'></i></a>
                        <% } %>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-12 mb-3 ">
                    <label class="form-label">Project description</label>
                    <textarea disabled rows="5" class="form-control" name="description"><%= singleProject.description %> </textarea>
                </div>
            </div>
        </form>
    </div>
</div>