<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
    <title><%= title %></title>
</head>
<body class="bodyLog">

    <main class="text-center form-signin w-100 m-auto">
        <% if (locals.messages.errors) { %> 
            <% messages.errors.forEach( el => { %>
                <div class="alert alert-danger"><%= el.msg %></div>
            <% }) %> 
        <% } %>
        <% if (locals.messages.info) { %>
            <% messages.info.forEach( el => { %>
                <div class="alert alert-info"><%= el.msg %></div>
            <% }) %>    
        <% } %>
        <form action="/login" method="POST">
          <img class="mb-4" src="https://i0.wp.com/www.datadeckdev.com/wp-content/uploads/2021/12/javascript.png?resize=1024%2C1024&ssl=1" alt="Project VITA logo" height="57">
          <h1 class="h3 mb-3 fw-normal">Please sign in</h1>
      
          <div class="form-floating">
            <input type="email" class="form-control" id="floatingLoginInput" placeholder="name@example.com" name="email">
            <label for="floatingLoginInput">Email address</label>
          </div>
          <div class="form-floating">
            <input type="password" class="form-control" id="floattingLoginPassword" placeholder="Password" name="password">
            <label for="floattingLoginPassword">Password</label>
          </div>
      
          <button class="w-100 btn btn-lg btn-dark" type="submit">Sign in</button>
          <p class="mt-2 pt-1">Don't have an account? <a href="/signup">Register</a></p>
          <p class="mt-4 mb-3 text-muted"><%= new Date().getFullYear() %> - © <a class="text-decoration-none text-reset" target="_blank" href="https://mauro-portfolio.netlify.app/">Mauro colucci</a></p>
        </form>
      </main>
        
</body>
</html>